universe-life:
  datasource:
    host: 43.142.103.248
    port: 3306
    username: root
    password: 1010520mao
    database: oauth2_authorization
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      database: 0
      host: 43.142.103.248
      port: 6379
      password: 1010520mao
      timeout: 10000
  auth:
    enable: true
    exclude-path:
      - /captcha/send
      - /captcha/verify
      - /api/upload/token
      - /api/upload/done
      - /api/download/token
      - /actuator/health
      - /actuator/info
      - /v3/api-docs
      - /favicon.ico
      - /swagger-ui.html
      - /error
      - /static/**
      - /css/**
      - /js/**
      - /html/**
  # Redisson 分布式锁配置（测试环境）
  redisson:
    # 基础配置
    password: 1010520mao
    connection-pool-size: 30
    connect-timeout: 8000
    timeout: 2500
    retry-attempts: 2
    retry-interval: 1000
    read-mode: MASTER
    subscription-mode: MASTER

    # 单机模式配置（测试环境推荐）
    single:
      enabled: true
      address: redis://43.142.103.248:6379
      database: 0
  mybatis-plus:
    type-aliases-package: com.universe.life.common.server.api.domain
  mail:
    app-name: 万象生活
    from: 18229923842@163.com
    app-url: http://43.142.103.248:3000

# 腾讯云 COS 配置
cos:
  secret-id: ${COS_SECRET_ID}
  secret-key: ${COS_SECRET_KEY}
  tmp-bucket: universelifetemp-1369282249
  formal-bucket: universelifeformal-1369282249
  region: ap-chongqing
  app-id: 100043346404
  sts-duration: 7200
  download-url-expiration: 1800



logging:
  level:
    org.springframework.amqp: INFO
    com.rabbitmq.client: INFO
    com.universe.life.common.server.api: DEBUG

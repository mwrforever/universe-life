server:
  port: 8099

# 授权服务配置
auth:
  service:
    # 授权服务器的 issuer URI，支持分布式部署
    # 如果不配置，将使用默认配置自动构建
    # 示例：http://auth-service.example.com:8099 或 http://192.168.1.100:8099
    issuer-uri: ${AUTH_SERVICE_ISSUER_URI:}

    # 服务端口配置（可选，默认值为 8099）
    server-port: ${SERVER_PORT:}

    # Nacos 注册的 IP 地址（可选，默认值为 localhost）
    nacos-ip: ${NACOS_IP:}
spring:
  profiles:
    active: dev
  application:
    name: auth-service
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: localhost:6848
        file-extension: yaml
        group: DEFAULT_GROUP
        namespace: a40a1d4b-4110-45e7-b16d-dc842e7ba324
        username: ${NACOS_ACCOUNT:nacos}
        password: ${NACOS_PASSWORD:nacos}
        shared-configs:
          - data-id: shared-datasource.yaml
            refresh: true
          - data-id: shared-redis.yaml
            refresh: true
          - data-id: shared-mybatis-plus.yaml
            refresh: true
          - data-id: shared-logging.yaml
            refresh: true
      discovery:
        server-addr: localhost:6848          # Nacos 注册中心地址
        namespace: a40a1d4b-4110-45e7-b16d-dc842e7ba324
        group: DEFAULT_GROUP
        username: ${NACOS_ACCOUNT:nacos}
        password: ${NACOS_PASSWORD:nacos}

# PowerJob Worker 配置
powerjob:
  worker:
    # Worker 端口（替换已弃用的 akka-port）
    port: 27777
    # 接入应用名称，用于分组隔离，推荐填写 本 Java 项目名称
    app-name: my-powerjob-worker
    # 调度服务器地址，IP:Port 或 域名，多值逗号分隔
    server-address: 127.0.0.1:7700
    # 通讯协议，支持 HTTP 和 AKKA 两种协议
    protocol: http
    # 持久化方式，可选：disk, memory（PowerJob 原生支持）
    store-strategy: disk
    # 任务返回结果信息的最大长度，超过这个长度的信息会被截断
    max-result-length: 4096
    # 单个任务追加的工作流上下文最大长度
    max-appended-wf-context-length: 4096
    # 同时运行的轻量级任务数量上限
    max-lightweight-task-num: 1024
    # 同时运行的重量级任务数量上限（修正属性名）
    max-heavyweight-task-num: 64
    # 健康报告间隔（秒）
    health-report-interval: 10
    # 允许延迟连接服务器
    allow-lazy-connect-server: false


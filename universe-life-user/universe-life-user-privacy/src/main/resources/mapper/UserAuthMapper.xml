<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.universe.life.user.privacy.mapper.UserAuthMapper">

    <select id="getUserInfo" resultType="com.universe.life.model.domain.dto.UserInfoDTO"
            parameterType="java.lang.String">
        select u.id         as id,
               u.username   as username,
               ua.password  as password,
               u.avatar_url as avatar
        from universe_life_user.user_auth ua
                 inner join universe_life_user.user u on ua.user_id = u.id
        where ua.identification = #{username};
    </select>
    <select id="getPasswordByUserId" resultType="com.universe.life.user.privacy.domain.dto.request.PasswordUserRequest"
            parameterType="java.lang.Long">
        select password
        from universe_life_user.user_auth
        where user_id = #{userId}
        limit 1;
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.universe.life.user.privacy.mapper.admin.AdminUserMapper">

    <select id="getAdminUserList" resultType="com.universe.life.user.privacy.domain.dto.AdminUserDTO">
        select
            id,
            username,
            avatar_url,
            gender,
            status,
            last_login_at ,
            last_login_ip ,
            created_at ,
            updated_at
        from universe_life_user.user
        where deleted = 0
        <if test="username != null">
            and username like concat('%', #{username}, '%')
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        <if test="gender != null">
            and gender = #{gender}
        </if>
        <if test="startTime != null">
            and created_at &gt; #{startTime}
        </if>
        <if test="endTime != null">
            and created_at &lt; #{endTime}
        </if>
    </select>
</mapper>

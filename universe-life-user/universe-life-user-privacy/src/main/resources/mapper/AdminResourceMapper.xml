<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.universe.life.user.privacy.mapper.AdminResourceMapper">

    <select id="selectPermissions" resultType="java.lang.String">
        select r.resource_code
        from resource r
                 inner join resource_role rr on r.id = rr.resource_id
                 inner join user_role ur on rr.role_id = ur.role_id
        where ur.user_id = #{userId}
          and r.deleted = 0
          and r.status = 1;
    </select>
</mapper>

server:
  port: 8094

universe-life:
  datasource:
    host: localhost
    port: 13306
    username: root
    password: 1010520mao
    database: universe_life_search
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      database: 0
      host: localhost
      port: 16379
      password: 1010520mao
      timeout: 10000
  # Redisson 分布式锁配置（测试环境）
  redisson:
    # 基础配置
    password: 1010520mao
    connection-pool-size: 30
    connect-timeout: 8000
    timeout: 2500
    retry-attempts: 2
    retry-interval: 1000
    read-mode: MASTER
    subscription-mode: MASTER

    # 单机模式配置（测试环境推荐）
    single:
      enabled: true
      address: redis://localhost:16379
      database: 1
  mybatis-plus:
    type-aliases-package: com.universe.life.search.domain

logging:
  level:
    org.springframework.amqp: INFO
    com.rabbitmq.client: INFO
    com.universe.life.search: DEBUG